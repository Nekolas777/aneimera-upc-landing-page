<html lang="en">
  <section class="header">
    <div class="xl:container mx-auto w-full md:w-2/3 text-center md:text-left">
      <div class="leading-relaxed mb-5">
        <h1 class="py-5 text-3xl text-white md:text-7xl font-semibold">ANEIMERA UPC</h1>
        <p class="py-5 md:text-2xl font-light text-white ">Fomentamos el desarrollo de la investigación mediante la transmisión de conocimientos, ideas y experiencias</p>
      </div>
      <div class="py-5">
        <a href="/eventos_page" class="py-3 px-7 font-semibold text-white bg-rojo rounded-full cursor-pointer">Únete a nuestros talleres</a>
      </div>
    </div>   
  </section>

  <style>
    .header {
    margin-top: 0;
    display: flex;
    align-items: center;

    height: 100vh;
    max-height: 800px;
    min-height: 640px;

    background-image: 
        /* Degradado sobre la imagen */
        linear-gradient(180deg, rgba(255, 255, 255, 0.00) 83.33%, #DFDFDF 100%),
        /* Imagen de fondo */
        linear-gradient(180deg, rgba(0, 0, 0, 0.50) 0%, rgba(0, 0, 0, 0.00) 100%), 
        linear-gradient(0deg, rgba(0, 0, 0, 0.30) 0%, rgba(0, 0, 0, 0.30) 100%), 
        url("../Imagenes/heroBackground.jpg");
    background-size: cover;
    background-position: top;
  
    @media (max-width: 730px) {
      padding: 2rem;
    }
}

.header h1 {
  line-height: 1.3;
}
    
    @media screen and (max-width: 768px) {
      .texto h2 {
        padding: 0;
        padding-bottom: 20px;
        font-size: 10px;
      }
      button p {
        font-size: 15px;
      }
	  }
    @media screen and (max-width: 400px) {
      button {
        padding: 10px 10px 10px 10px;
      }
	  }
  </style>

  <script>
    var navbar = document.querySelector('header');
    var navbarHeight = navbar!.offsetHeight;
    var hero = document.getElementById('hero');
    var computedStyles = window.getComputedStyle(hero);
    var currentHeight = parseFloat(computedStyles.height);
    var newHeight = currentHeight - navbarHeight;
    hero.style.height = newHeight + "px";
  </script>

</html>
